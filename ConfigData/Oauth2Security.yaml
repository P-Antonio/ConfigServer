server:
  port: ${PORT:${SERVER_PORT:0}}

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

keycloak:
  client-id: spring-client
  client-secret: J7dDeQlfxoRMdg5aDKZWJIZ8Tk1EuPkA
  token-uri: http://localhost:9090/realms/Keycloak-realm-dev/protocol/openid-connect/token
  user-registration-uri: http://localhost:9090/admin/realms/Keycloak-realm-dev/users
  admin-username: admin
  admin-password: admin