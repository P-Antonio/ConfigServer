server:
  port: 8888

spring:
  application:
    name: Config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/P-Antonio/ConfigServer
        search-paths: ConfigData
