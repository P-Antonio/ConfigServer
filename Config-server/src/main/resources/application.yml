server:
  port: 8888

spring:
  application:
    name: Config-server
  cloud:
    config:
      server:
        git:
          default-label: config_server
          uri: https://github.com/P-Antonio/ConfigServer
          search-paths: ConfigData
          clone-on-start: true
