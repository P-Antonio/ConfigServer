server:
  port: 8888

spring:
  application:
    name: Config-server
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/P-Antonio/ConfigServer/tree/config_server
          search-paths: ConfigData
